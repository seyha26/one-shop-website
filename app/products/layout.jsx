export function metadata () {
    return {
        title: "Products | One Shop"
    }
}

export default function Layout ({children}) {
    return (
        <main>
            {children}
        </main>
    )
}